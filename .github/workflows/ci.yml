# name: CI Build and Test

# on:
#   workflow_dispatch:

# env:
#   DOTNET_VERSION: '10.0'
#   NODE_VERSION: '20'

# jobs:
#   build-backend:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Setup .NET
#       uses: actions/setup-dotnet@v4
#       with:
#         dotnet-version: ${{ env.DOTNET_VERSION }}

#     - name: Restore dependencies
#       run: dotnet restore ReferenceRAG.slnx

#     - name: Build
#       run: dotnet build ReferenceRAG.slnx --no-restore --configuration Release

#     - name: Run Tests
#       run: dotnet test ReferenceRAG.slnx --no-build --configuration Release --verbosity normal

#   build-frontend:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Setup Node.js
#       uses: actions/setup-node@v4
#       with:
#         node-version: ${{ env.NODE_VERSION }}
#         cache: 'npm'
#         cache-dependency-path: dashboard-vue/package-lock.json

#     - name: Install dependencies
#       run: npm ci
#       working-directory: ./dashboard-vue

#     - name: Type check
#       run: npm run type-check
#       working-directory: ./dashboard-vue

#     - name: Build
#       run: npm run build
#       working-directory: ./dashboard-vue

#   docker-build:
#     runs-on: ubuntu-latest
#     needs: [build-backend, build-frontend]

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3

#     - name: Login to GitHub Container Registry
#       uses: docker/login-action@v3
#       with:
#         registry: ghcr.io
#         username: ${{ github.actor }}
#         password: ${{ secrets.GITHUB_TOKEN }}

#     - name: Build and push
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         push: ${{ github.event_name != 'pull_request' }}
#         tags: |
#           ghcr.io/${{ github.repository }}/reference-rag:latest
#           ghcr.io/${{ github.repository }}/reference-rag:${{ github.sha }}
#         cache-from: type=gha
#         cache-to: type=gha,mode=max
